.build:
  variables:
    GRADLE_USER_HOME: $CI_PROJECT_DIR/.gradle
  stage: build
  script:
    - ./gradlew --warning-mode=all build
  cache:
    - key:
        files:
          - gradle/wrapper/gradle-wrapper.properties
      paths:
        - .gradle/wrapper
    - key: "${CI_COMMIT_REF_SLUG}_${CI_JOB_NAME_SLUG}"
      fallback_keys:
        - "${CI_DEFAULT_BRANCH_SLUG}_${CI_JOB_NAME_SLUG}"
      paths:
        - .gradle/caches
#   artifacts:
#     when: always
#     paths:
#       - build/test-results/
#       - build/reports/jacoco/
#     reports:
#       junit: build/test-results/**/TEST-*.xml
#       coverage_report:
#         coverage_format: jacoco
#         path: build/reports/jacoco/**/*.xml

build:java8:
  image: eclipse-temurin:8-jdk
  extends: .build
  allow_failure: true

build:java11:
  image: eclipse-temurin:11.0.29_7-jdk
  extends: .build
  allow_failure: true

build:java17:
  image: eclipse-temurin:17.0.17_10-jdk
  extends: .build

build:java21:
  image: eclipse-temurin:21.0.9_10-jdk
  extends: .build

build:java25:
  image: eclipse-temurin:25.0.1_8-jdk
  extends: .build
  allow_failure: true
